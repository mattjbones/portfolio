<div class="photo-card">
  <a href="{{ post.url }}">
    <img src="{{ post.thumb }}" alt="{{ post.title }}" loading="lazy">
    <div class="photo-card-body">
      <div class="photo-card-title">{{ post.title }}</div>
      <div class="photo-card-meta">{{ post.film }} — {{ post.location }}</div>
    </div>
  </a>
</div>
